# Скопируйте этот файл в .env и заполните значениями из Firebase
# (Settings -> General -> Your apps -> Web app).
# .env и firebase-config.js не попадают в репозиторий (см. .gitignore).
# Для деплоя на GitHub Pages конфиг берётся из секрета репозитория FIREBASE_CONFIG.
FIREBASE_CONFIG=
